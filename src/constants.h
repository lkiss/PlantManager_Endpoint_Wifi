#ifndef CONFING_FILE_PATH
#define CONFING_FILE_PATH "/config/config.json"
#endif

const IPAddress LOCAL_IP(192, 168, 1, 100);
const IPAddress GATEWAY(192, 168, 1, 1);
const IPAddress SUBNET(255, 255, 255, 0);